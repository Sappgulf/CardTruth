# Grader references moved
Packaged references live in `cardtruth_core/profiles/*.json`. The 0.1 arbitrary defect weights, standard deviations and unsupported grade caps were removed. These files are centering-only reference checks, not grading algorithms. BGS numerical rules remain unavailable pending source verification.
